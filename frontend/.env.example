# For local dev: leave unset to use localhost:8000
# For Vercel: set this to your deployed backend URL (Railway, Render, etc.)
NEXT_PUBLIC_API_URL=https://your-backend.railway.app
# Or use BACKEND_URL (both work for rewrites)
